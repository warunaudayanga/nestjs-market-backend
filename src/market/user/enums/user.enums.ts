// noinspection JSUnusedGlobalSymbols

export enum Gender {
    MALE = "Male",
    FEMALE = "Female"
}

export enum Role {
    ADMIN = "Admin",
    USER = "User"
}

export enum StatusString {
    ACTIVE = "Active",
    DEACTIVE = "Deactive",
    DELETED = "Deleted"
}
